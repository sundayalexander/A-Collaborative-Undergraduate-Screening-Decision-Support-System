<!--left-fixed -navigation-->
<div class="sidebar" role="navigation">
    <div class="navbar-collapse">
        <nav class="cbp-spmenu cbp-spmenu-vertical cbp-spmenu-left" id="cbp-spmenu-s1">
            <ul class="nav" id="side-menu">
                <li>
                    {% if page ends with "dashboard" %}
                    <a href="{{ path('student_dashboard') }}" class="active"><i class="fa fa-dashboard nav_icon"></i>Dashboard</a>
                    {% else %}
                        <a href="{{ path('student_dashboard') }}"><i class="fa fa-dashboard nav_icon"></i>Dashboard</a>
                    {% endif %}
                </li>
                <li>
                    {% if page ends with "admin-unit" %}
                    <a href="{{ path('student_admin_unit') }}" class="active"><i class="fa fa-book nav_icon"></i>Admission Unit</a>
                    {% else %}
                        <a href="{{ path('student_admin_unit') }}"><i class="fa fa-book nav_icon"></i>Admission Unit</a>
                    {% endif %}
                    <!-- /nav-second-level -->
                </li>
                <li>
                    {% if page ends with "health-service" %}
                        <a href="{{ path('student_health_service') }}" class="active"><i class="fa fa-hospital-o nav_icon"></i>University Health Service</a>
                    {% else %}
                    <a href="{{ path('student_health_service') }}"><i class="fa fa-hospital-o nav_icon"></i>University Health Service</a>
                    {% endif %}
                </li>
                <li>
                    {% if page ends with "faculty" %}
                        <a href="{{ path('student_faculty') }}" class="active"><i class="fa fa-bookmark-o nav_icon"></i>Faculty</a>
                    {% else %}
                    <a href="{{ path('student_faculty') }}"><i class="fa fa-bookmark-o nav_icon"></i>Faculty</a>
                    {% endif %}
                </li>
                <li>
                    {% if page ends with "student_affairs" %}
                        <a href="{{ path('student_affairs') }}" class="active" ><i class="fa  fa-group nav_icon"></i>Student Affairs</a>
                    {% else %}
                    <a href="{{ path('student_affairs') }}" ><i class="fa  fa-group nav_icon"></i>Student Affairs</a>
                    {% endif %}
                </li>
                <li>
                    {% if page ends with "exams_and_records" %}
                        <a href="{{ path('student_exams&records') }}" class="active" ><i class="fa fa-paperclip nav_icon"></i>Exams &amp; Records</a>
                    {% else %}
                    <a href="{{ path('student_exams&records') }}" ><i class="fa fa-paperclip nav_icon"></i>Exams &amp; Records</a>
                    {% endif %}
                </li>
                <li>
                    <a href="{{ path('student_logout') }}" style="color: red;"><i class="fa fa-sign-out nav_icon"></i>Logout</a>
                </li>
            </ul>
            <!-- //sidebar-collapse -->
        </nav>
    </div>
</div>
<!--left-fixed -navigation-->
<style>
    .form-body form ul li{
        color: red;
        list-style: none;
        padding: 0em .1em;
        font-size: small;
    }
</style>
<!-- header-starts -->
<div class="sticky-header header-section ">
    <div class="header-left">
        <!--toggle button start-->
        <button id="showLeftPush"><i class="fa fa-bars"></i></button>
        <!--toggle button end-->
        <!--logo -->
        <div class="logo">
            <a href="{{ path('student_dashboard') }}">
                <h1>ONLINE UNDERGRADUATE STUDENT'S</h1>
                <span> SCREENING DECISION SUPPORT SYSTEM</span>
            </a>
        </div>
        <!--//logo-->
        <div class="clearfix"> </div>
    </div>
    <div class="header-right">
        <!--notification menu end -->
        <div class="profile_details">
            <ul>
                <li class="dropdown profile_details_drop">
                    <a href="{{ path('student_login') }}" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false">
                        <div class="profile_img">
                            <span class="prfil-img"><img src="{{ asset('img/favicon/apple-icon-60x60.png') }}" alt="logo"> </span>

                        </div>
                    </a>
                </li>
            </ul>
        </div>
        <div class="clearfix"> </div>
    </div>
    <div class="clearfix"> </div>
</div>
<!-- //header-ends -->